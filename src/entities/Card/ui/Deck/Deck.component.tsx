import { View, Text } from 'react-native';
import React from 'react';
import { DeckProps } from './Deck.types';

export default function Deck({}: DeckProps) {
    
  return (
    <View>
      <Text>Deck</Text>
    </View>
  );
}
